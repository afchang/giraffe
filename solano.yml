java:
    java_version: 'java-7-openjdk'
    gradle_version: '2.4'
cache:
    key_paths:
    - build.gradle
    save_paths:
    - .gradle
tests:
    - type: junit
      command: ./gradlew build --console=plain
      report_files:
          - "**/build/test-results/*.xml"
